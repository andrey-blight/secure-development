# ServiceAccount for SecDev Course App
# Minimal permissions - no automatic token mounting
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secdev-app-sa
  namespace: secdev
  labels:
    app: secdev-app
automountServiceAccountToken: false
